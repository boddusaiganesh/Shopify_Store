{
  "name": "shopify-ingestion-service",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
